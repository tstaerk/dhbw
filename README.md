This app allows to query an LLM using langchain. To use it:
* create an account at streamlit
* connect this account with this github repo
* add a secret that contains the OpenAI key
Then it should run.
